# INFO-5940 Assignment 2 - Simple Project

This is a small project for the INFO-5940 class. It contains a tiny set of scripts and sample data used for assignment 2. The README is written in a simple student style.

## What this project does

The project stores some example text data and a short Python script used for the assignment. It is meant for learning and demonstration purposes.

## Files in this repository

- `assign_2.py` - main Python script for the assignment (student code)
- `test_apikey.py` - a small test file (example)
- `requirements.txt` - Python dependencies (if needed)
- `data/combined_transcript.txt` - example combined transcript data
- `data/RAG_source.txt` - example source file for retrieval/augmented generation
- `README.md` - this file
- `OLD_README.md`, `ref-log.md` - extra notes and logs

## How to run (simple)

1. Make sure you have Python 3 installed.
2. (Optional) Create and activate a virtual environment:

	python3 -m venv venv
	source venv/bin/activate

3. Install dependencies (if needed):

	pip install -r requirements.txt

4. Run the main script:

	python assign_2.py

Note: The script may be a student submission and could require small edits to run in different environments.

## Data

The `data` folder contains text files used by the assignment. They are small and for demo only. You can open them with any text editor.

## Author

Written by a student for INFO-5940.

## License / Notes

This is a class assignment. Feel free to read and run the code for learning purposes.

If you want improvements, you can add a better description of `assign_2.py` and examples of expected input/output.